def recurse(str):
    for i in enumerate


with open('test.txt') as raw_input:
    add_up = []
    for i in raw_input:
        add_up.append(recurse(i))
        # print(recurse(i))
    # sum = 0
    # for j in add_up:
    #     sum += j
    # print("Sum: ", sum)